# Dev image: installs devDependencies (vite, tsx) and bind-mounts source.
FROM node:20-alpine
RUN corepack enable
WORKDIR /app
COPY package.json pnpm-lock.yaml* ./
RUN pnpm install --frozen-lockfile || pnpm install
EXPOSE 8080
CMD ["pnpm", "dev"]
